// !Creating arrays
var sampleArrays = ["Ivan", "Gregor", "Tabalno"];

// !Getting the specific item in arrays
var sampleItem = sampleArrays[1];

// !Getting the length of arrays
var sampleLength = sampleArrays.length;

// !To know if the item is included inside arrays
sampleArrays.includes("Gregor");
